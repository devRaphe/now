#include "../Flutter/Generated.xcconfig"

BUNDLE_NAME = BoroMe
BUNDLE_ID = ng.borome.ios
BUNDLE_ID_APPEND =
PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_ID)$(BUNDLE_ID_APPEND)

VERSION_APPEND =
BUNDLE_VERSION = $(FLUTTER_BUILD_NUMBER)$(VERSION_APPEND)
